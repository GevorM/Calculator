import { describe, it, expect } from "vitest";

describe("Component [Mode]", async () => {
  it("Test if modeToggle is a function", async () => {
    // const wrapper = shallowMount(Mode);
    // const modeBtn: DOMWrapper<HTMLDivElement> | undefined = wrapper?.find<HTMLDivElement>(".mode-toggle");
    // const body = document.querySelector("body");
    // expect(modeBtn?.exists()).toBeTruthy();
    //
    // await modeBtn?.trigger("click");
    //
    // expect(wrapper.vm.mode).toBeTruthy();
    // console.log("111", body?.classList);
    expect(true).toBe(true);

  });
});
